# 自動化系統結案報告與運作邏輯說明書

## 1. 系統架構總覽 (System Architecture)

本專案旨在自動化 **「每日文章資料搜集與歸檔」** 流程。系統由兩大核心組件構成：
1.  **n8n 自動化工作流程 (Local Host)**：負責抓取、解析、與 Google Drive API 溝通，最後將資料寫入試算表。
2.  **Google Sheets 自動化腳本 (Apps Script)**：負責資料寫入後的「最後一哩路」，包括格式優化與視覺整理。

---

## 2. n8n 工作流程邏輯 (Workflow Logic)

### 2.1 觸發與資料來源 (Trigger & Source)
- **觸發**:手動執行或排程 (Cron)。
- **資料處理**: 讀取原始 HTML 或 RSS 來源，解析出文章標題、品牌、日期等資訊。

### 2.2 Google Drive 智慧搜尋 (Drive API Integration)
由於 n8n 內建的 Google Drive 節點在地端環境有限制，我們採用 **HTTP Request 節點** 直接對接 **Google Drive API v3**。

- **邏輯步驟**:
    1. **提取父資料夾**: 鎖定目標父資料夾 ID (`1OaXcIEY0CXDTHL4TzBbfvwrMUeaXrzPD`)。
    2. **動態路徑判斷**: 依據 `Brand` 欄位決定子資料夾：
        - `Asia KOL` -> 使用特定 ID。
        - `Fans Feed` -> 使用特定 ID。
        - 其他 -> 使用預設 ID (`i-Buzz消費者洞察`)。
    3. **日期比對搜尋**:
        - API 查詢語法：`name contains 'YYYYMMdd'` (使用 `{{ $today.format('yyyyMMdd') }}` 動態生成)。
        - 額外篩選：`mimeType = 'application/vnd.google-apps.folder'` (確保只找資料夾)。
    4. **輸出結果**: 取得該日期資料夾的 `WebViewLink` (雲端連結)。

### 2.3 資料寫入 (Google Sheets Node)
- 將整理好的文章資訊 (包含剛取得的雲端連結) 寫入 Google Sheets 的最新一列。
- 此動作會觸發 Google Sheets 的 `On Change` 事件。

---

## 3. Google Apps Script 運作邏輯 (Post-Processing)

當 n8n 寫入資料後，Google Sheets 腳本會自動接手進行格式整理。目前採用的是 **v17 (Incremental Merge)** 版本。

### 核心功能 1：自動核取方塊 (Auto-Checkboxes)
- **範圍**: D 欄至 H 欄 (官網、電子報、首頁、媒體、臉書)。
- **邏輯**:
    - 檢查新增資料列的 D~H 欄。
    - 若內容為文字 "TRUE" / "FALSE"，自動轉換為 **互動式 Checkbox**。
    - 統一設為 TRUE (打勾狀態)。

### 核心功能 2：增量日期合併 (Incremental Date Merge)
為了解決重複日期顯示雜亂的問題，但又不希望每次重新整理整張表 (耗時且風險高)，我們設計了 **「增量合併」** 演算法。

- **觸發對象**: 只針對 **「最後一列 (最新寫入的那筆資料)」**。
- **邏輯流程**:
    1. **讀取**: 讀取最新一列 (Target Row) 的日期。
    2. **比對**: 讀取 **「上一列 (Previous Row)」** 的日期。
    3. **判斷**:
        - **若日期相同**: 表示這筆資料屬於今天的群組。
            - 程式會找出上一列所屬合併範圍的 **「起始列 (Head)」**。
            - 將最新一列 **「融合 (Merge)」** 進這個範圍。
        - **若日期不同**: 表示這是新的一天 (新的群組)。
            - 程式 **不做任何動作** (保持獨立)。
- **優勢**:
    - **極速**: 永遠只處理 1-2 個儲存格，資料再多也不會變慢。
    - **安全**: 絕對不會去動到上面已經整理好的歷史資料。

---

## 4. 技術與維護指南 (Technical Guide)

### 4.1 關鍵參數
- **Google Drive 父資料夾 ID**: `1OaXcIEY0CXDTHL4TzBbfvwrMUeaXrzPD`
- **Apps Script 觸發器**: 必須設定為 `On Change` (變更時)。

### 4.2 常見問題排除
- **Q: 為什麼日期沒合併？**
    - A: 請確認 n8n 寫入的日期格式是否與上一列完全一致 (包含全形半形、斜線格式)。v17 版只會合併「顯示文字」完全相同的格子。
- **Q: 為什麼 Drive 連結沒出現？**
    - A: 請檢查 n8n 的 HTTP Request 節點輸出，若當天沒有對應日期的資料夾，連結欄位將會留空。

---

**專案狀態**: ✅ 已完成驗證與部署
**文件版本**: v1.0
**最後更新**: 2026/02/10
